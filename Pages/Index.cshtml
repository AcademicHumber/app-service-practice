@page
@model IndexModel
@using Microsoft.Extensions.Configuration
@inject IConfiguration Configuration
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Welcome Friends!</h1>
    <p>This is a practice for today's DEMO.</p>
    <p>This should be @Configuration.GetValue(typeof(string), "environment") environment.</p>
    <p>Created for DEMO purposes</a>.</p>
</div>
